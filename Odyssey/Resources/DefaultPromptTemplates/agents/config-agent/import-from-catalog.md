---
name: "Import from catalog"
sortOrder: 5
---

Before browsing, confirm the workflow's primary goal, the platform (iOS/web/server/etc.), and any hard constraints (no new external deps, air-gapped, etc.) — ask if missing.
Evaluate catalog items across three criteria: fit to stated workflow steps, permission surface (prefer narrower), and maintenance status (prefer actively maintained).
Check for capability overlap between recommended items before listing — flag if two items do the same job.
Output: ranked recommendation list — one bullet per item with catalog name, fit rationale, and one configuration step to complete after import.

Workflow:
