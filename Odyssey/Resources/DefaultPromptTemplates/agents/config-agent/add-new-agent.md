---
name: "Add a new agent"
sortOrder: 1
---

Before proposing anything, confirm the agent's primary trigger (user-invoked vs. autonomous), target platform, and whether it needs write access to files or external services — ask if not stated.
Propose three things: a minimal skill set (name + one-line purpose each), the narrowest MCP permissions that satisfy the role, and any hooks needed for pre/post actions.
Check that no proposed permission is broader than the agent's stated tasks; flag over-grants explicitly.
Output: one section each for skills, MCP permissions, and hooks; close with the one integration to wire first.

Role:
